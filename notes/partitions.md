# Partitions

We can parition data to make sure that it is distributed evenly or certain nodes always get same data.

A bad partition can hurt because it can reduce parallelism and can also overwork a certain subset of nodes

